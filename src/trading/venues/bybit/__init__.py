"""Bybit V5 venue adapter (REST + WebSocket) — Phase 0 live venue.

Wire-format alignment: docs/BYBIT_ABC_ALIGNMENT.md
Contract smoke test:   scripts/bybit_testnet_smoke.py

BybitVenue is added once REST slices land; until then this package only
exposes the pure conversion helpers in `adapters`.
"""
